serve:
  cookies:
    same_site_mode: Lax

urls:
  self:
    issuer: http://hydra.localhost
  consent: http://login.localhost/consent
  login: http://login.localhost/login
  logout: http://login.localhost/logout

secrets:
  system:
    - youReallyNeedToChangeThis

oidc:
  subject_identifiers:
    supported_types:
      - pairwise
      - public
    pairwise:
      salt: youReallyNeedToChangeThis
